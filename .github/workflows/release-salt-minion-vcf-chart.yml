name: release (salt-minion-vcf chart)

# Tag scheme: salt-minion-vcf-chart/vX.Y.Z -- must match the `version:` in
# salt-minion-vcf/helm/salt-minion-vcf/Chart.yaml. See docs/releasing.md.
on:
  push:
    tags:
      - "salt-minion-vcf-chart/v*"

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: Derive version and verify it matches Chart.yaml
        id: meta
        run: |
          set -eu
          tag_version="${GITHUB_REF_NAME#salt-minion-vcf-chart/v}"
          chart_version="$(helm show chart salt-minion-vcf/helm/salt-minion-vcf | awk '/^version:/ {print $2}')"
          if [ "$tag_version" != "$chart_version" ]; then
            echo "Tag version $tag_version does not match Chart.yaml version $chart_version" >&2
            exit 1
          fi
          grep -Eq "^#+ salt-minion-vcf-chart/v${tag_version}$" salt-minion-vcf/helm/salt-minion-vcf/CHANGELOG.md \
            || { echo "No CHANGELOG.md entry for salt-minion-vcf-chart/v${tag_version}" >&2; exit 1; }
          echo "version=$tag_version" >> "$GITHUB_OUTPUT"
      - name: helm lint
        run: helm lint salt-minion-vcf/helm/salt-minion-vcf
      - name: helm package
        run: helm package salt-minion-vcf/helm/salt-minion-vcf
      - name: helm registry login
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: helm push
        run: helm push "salt-minion-vcf-${{ steps.meta.outputs.version }}.tgz" "oci://ghcr.io/${{ github.repository }}/charts"
