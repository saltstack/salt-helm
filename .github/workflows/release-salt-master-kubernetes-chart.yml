name: release (salt-master-kubernetes chart)

# Tag scheme: salt-master-kubernetes-chart/vX.Y.Z -- must match the
# `version:` in salt-master-kubernetes/Chart.yaml. See docs/releasing.md.
on:
  push:
    tags:
      - "salt-master-kubernetes-chart/v*"

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: Derive version and verify it matches Chart.yaml
        id: meta
        run: |
          set -eu
          tag_version="${GITHUB_REF_NAME#salt-master-kubernetes-chart/v}"
          chart_version="$(helm show chart salt-master-kubernetes | awk '/^version:/ {print $2}')"
          if [ "$tag_version" != "$chart_version" ]; then
            echo "Tag version $tag_version does not match Chart.yaml version $chart_version" >&2
            exit 1
          fi
          grep -Eq "^#+ salt-master-kubernetes-chart/v${tag_version}$" salt-master-kubernetes/CHANGELOG.md \
            || { echo "No CHANGELOG.md entry for salt-master-kubernetes-chart/v${tag_version}" >&2; exit 1; }
          echo "version=$tag_version" >> "$GITHUB_OUTPUT"
      - name: helm lint
        run: helm lint salt-master-kubernetes
      - name: helm package
        run: helm package salt-master-kubernetes
      - name: helm registry login
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: helm push
        run: helm push "salt-master-kubernetes-${{ steps.meta.outputs.version }}.tgz" "oci://ghcr.io/${{ github.repository }}/charts"
